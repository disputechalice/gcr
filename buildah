#!/bin/bash

c=$(buildah from docker.io/library/debian:12)

buildah config --env DEBIAN_FRONTEND=noninteractive "$c"

buildah run "$c" bash -v -c '
    apt-get -qq update
    apt-get -qqo Dpkg::Use-Pty=0 satisfy
        wget
'

buildah commit "$c" gcr:latest